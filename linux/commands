dmesg -HTx ==> stores system information

vmstat

pidstat

lscpu
dmidecode

mtr ==> real time network monitoring  ==> e.g. mtr google.com 

mpstat ==> check every cpu usuage

iostat  ==> check disk status

free -m ==> free memory  (free -g, free -m , free -k)

ifconfig

ip r

ss ==> check connection performance

iftop ==> check network performance

smem ==> check memory allocated per process
ss  ==> system socket

sudo netstat -tunlp

telnet

env

lsof ==> list of open files

df 

id

w

nslookup

uptime

wall hello  ==> broadcast message to users

who

sort

shutdown -h now

shutdown -r now ==> restart

whatis

find

fuser

lsblk

systecmctl is-enabled httpd
systecmctl disabled httpd
systecmctl mask httpd    ==> not able to start
systecmctl unmask httpd
systecmctl poweroff
systecmctl reboot


alias ==> create shortcup
alias -p
alias shortcut="actual command"

.bashrc ==> for current user
/etc/bashrc ===> for all users

rsync ===> used to send big file, if updated , it will send only updated contents

selinux  ==> sestatus or getenforce  ==> setenforce=0 / setenforce=1
modify in sudo vi /etc/selinux/config 


logrotate

dnsserver


ACL ==> access control list ==> setfacl, getfacl


env

shred ==> permenently delete file ==> shred -u filename, shred --remove finename

open port ==> netstat -putan | grep port_no

kill ==> kill pid, kill -1 pid (restart) , kill -2 pid (interrupt from keyboard) , kill -9 pid (forcefully terminate), kill -15 pid (graceful termination)

jobs ==> list running jobs
bg ==> send process to background
fg ==> get process to foreground


suid, sgid

sticky bit ==> if set only owner of file/directory can delete/rename file

gpasswd ==> remove user from group
usermod -aG groupname username


crontab

at ==> schedule job once, at hh:mm, atq list jobs, atrm remvoe jobs, at 4:45 am, at 4pm + 4 days, at now + 5 hours, at 9:00am tommorow, at 10:00 am next month, at 10:00 am nov 30

hostnamectl set-hostname newname

create large file ==> fallocate -l 35m filename

httpd server in centos ==> conf--> /etc/httpd/conf/httpd.conf, contents --> /var/www/html/, logs --> /var/log/httpd

nginx ==> html file --> sudo vi /usr/share/nginx/html/, config file -->  sudo vi /etc/nginx/nginx.conf

command for dns resolution  ==> ping, , nslookup, dig, host

pidof httpd

ps -l pid

ln --> make soft and hard link


echo $RANDOM ==> random number generator

lvm ==> logical volume manager
